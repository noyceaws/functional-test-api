[tool:pytest]
markers =
    unit: Unit tests that don't require a running server
    functional: Functional tests that require a running server
    positive: Tests that verify expected successful behavior
    negative: Tests that verify error handling and validation
